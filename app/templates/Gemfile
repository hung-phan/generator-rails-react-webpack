#
# Generated by generator-rails-react-webpack
#

gem 'bootstrap-sass' # twitter bootstrap<% if (includeModernizr) { %>
gem 'modernizr-rails' # modernizr<% } %><% if (includeMongodb) { %>
gem 'mongoid' # mongoid<% } %>

group :development, :test do
  gem 'faker' # https://github.com/stympy/faker
  gem 'factory_girl_rails' # https://github.com/thoughtbot/factory_girl_rails
  gem 'database_cleaner'
  gem 'rspec'
  gem 'rspec-rails' # https://github.com/rspec/rspec-rails
  gem 'guard'
  gem 'guard-rspec', require: false
  gem 'shoulda-matchers' # https://github.com/thoughtbot/shoulda-matchers
  gem 'shoulda-callback-matchers', '~> 1.1.1'
  gem 'spring-commands-rspec'
  gem 'mailcatcher' # mailcatcher for development mode
end

group :development do
  gem 'better_errors'
  gem 'binding_of_caller'
  gem 'meta_request'
end

# group :test do
#   gem 'airborne' # api test
# end

gem 'react-rails' # react jsx compile<% if (includeFontAwesome) { %>
gem 'font-awesome-rails' # font awesome<% } %><% if (includeGrape) { %>
gem 'grape-entity'
gem 'grape' # grape api<% } %>
