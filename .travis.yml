language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-rails-react-webpack' ]; then cd .. && eval "mv $currentfolder generator-rails-react-webpack" && cd generator-rails-react-webpack; fi
